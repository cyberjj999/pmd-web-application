@model PMDWebApplication.Models.SysAdminRegisterViewModel
@{
    ViewBag.Title = "SysAdminRegister";
}

@{
    ViewBag.Title = "Register";
    Layout = null;
}


@using (Html.BeginForm("RegisterNewUser", "SystemAdmin", FormMethod.Post, new { @class = "form-horizontal", id = "myForm", role = "form" }))
{
    @Html.AntiForgeryToken()
    <p style="color:red">
        This is to register new users with a particular role. Upon registration, the
        account's credentials will be sent to the email entered.
    </p>
    @Html.ValidationSummary("", new { @class = "text-danger" })
    @*
        <div id="ifErrorOccurs">
            @Html.ValidationMessage("", "You need to", new { @class = "text-danger" })
        </div>
    *@
    <div class="form-group">
        @Html.LabelFor(m => m.RoleName, new { @class = "col-md-6 control-label" })
        <div class="col-md-10">
            @Html.DropDownListFor(m => m.RoleName, new SelectList(ViewBag.Roles, "Value", "Text"), new { @class = "form-control" })
        </div>
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.UserName, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.UserName, new { @class = "form-control" })

        </div>
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.FullName, new { @class = "col-md-4 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.FullName, new { @class = "form-control" })

        </div>
    </div>


    <div class="form-group">
        @Html.LabelFor(m => m.Email, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })

        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" id="btnSubmit" value="Register" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

<script src="~/Scripts/jquery.validate.min.js" type="text/javascript"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js" type="text/javascript"></script>

@*@Html.ActionLink("Back", "Index", "Manage", null, new { @class = "btn btn-default" }) *@

<script>


    /*        $(document).ready(function () {
                $("#myForm").submit(function () {
                    alert("yesees");
                    var myformdata = $("#myForm").serialize();

                    $.ajax({
                        type: "POST",
                        url: "/Manage/RegisterNewUser",
                        data: myformdata,

                        success: function () {
                            alert("hohoho");
                           // $("#loaderDiv").hide();
                           // $("#myModal1").modal("hide");
                            window.location.href = "/Manage/RegisterNewUser";
                        },
                        error: function () {
                            alert("error");
                        }

                    })
                })
            })*/
    /*
        $.ajax({//other options
            success: function (data) {
                if (data.Status == 1) {
                    alert("no problemo");
                    //successfull
                }
                else if (data.Status == 2) {
                    alert(data.Message);
                }
            }
        });*/

</script>
@*
        <script src="~/Scripts/jquery.validate.min.js" type="text/javascript"></script>
        <script src="~/Scripts/jquery.validate.unobtrusive.min.js" type="text/javascript"></script>
        @section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }
*@
@*
    <script>
        $(document).ready(function () {
            $("#btnSubmit").click(function () {

                $("#loaderDiv").show();

                var myformdata = $("#myForm").serialize();

                $.ajax({

                    type: "POST",
                    url: "/Manage/RegisterNewUser",
                    data: myformdata,
                    success: function () {
                        $("#loaderDiv").hide();
                        $("#myModal1").modal("hide");
                        window.location.href = "/Manage/RegisterNewUser";
                    }

                })
            })
        })
    </script>*@


